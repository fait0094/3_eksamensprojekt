---
import Button from "./Button.astro";
import "..//styles/style.css";
const { title, description, imgPath, kategori, link_side } = Astro.props;
---

<section class="">
  <div class="rounded-lg w-96 shadow-xl bg-primary_white">
    <figure class="px-10 pt-10">
      <img src={imgPath} alt="hund_til_adoption" />
    </figure>
    <div class="p-10">
      <div class="">
        <h2 class="uppercase">{kategori}</h2>
        <div class="flex justify-between">
          <h3 class="uppercase">{title}</h3>
          <slot />
        </div>
      </div>
      <p>{description}</p>
      <div class="my-6">
        <Button variant="primary_blue" size="medium" link_side="">
          Læs mere
        </Button>
      </div>
    </div>
  </div>
</section>
